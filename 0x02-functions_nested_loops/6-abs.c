#include "main.h"

/**
 * _abs - checks for absolute value
 *
 * @num: parameter to be checked
 *
 * Return: always -num
 */

int _abs(int num)
{
	{
		if (num >= 0)
			return (num);
	}
	else if
	{
		return (-num);
	}
}
